# Frontend Environment Configuration
# In local development: leave empty to use the Vite dev proxy (/api/contact -> http://localhost:5000/api/contact)
# In production (Vercel): set to your deployed Railway backend URL
# Example: VITE_API_URL=https://your-railway-backend.up.railway.app
VITE_API_URL=
